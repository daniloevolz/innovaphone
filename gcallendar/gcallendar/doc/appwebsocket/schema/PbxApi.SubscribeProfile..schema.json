{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$id": "pbxapi.subscribeprofile.schema.json",
  "description": "Starts monitoring the first profile of the user that established the AppWebsocket connection.",
  "type": "object",
  "required": [
    "api",
    "mt"
  ],
  "properties": {
    "api": {
      "type": "string",
      "enum": [
        "PbxApi"
      ],
      "description": "To address this interface the value 'PbxApi' has to be used for the api property"
    },
    "mt": {
      "type": "string",
      "enum": [
        "SubscribeProfile"
      ],
      "description": "The message type identifies the requested operation"
    }
  },
  "additionalProperties": false
}