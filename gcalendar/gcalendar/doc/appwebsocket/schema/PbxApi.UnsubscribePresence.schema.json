{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$id": "pbxapi.unsubscribepresence.schema.json",
  "description": "Stop monitoring of the presence of the object defined by sip or num.",
  "type": "object",
  "required": [
    "api",
    "mt",
    "sip",
    "num"
  ],
  "properties": {
    "api": {
      "type": "string",
      "enum": [
        "PbxApi"
      ],
      "description": "To address this interface the value 'PbxApi' has to be used for the api property"
    },
    "mt": {
      "type": "string",
      "enum": [
        "UnsubscribePresence"
      ],
      "description": "The message type identifies the requested operation"
    },
    "sip": {
      "type": "string"
    },
    "num": {
      "type": "string"
    }
  }
}