{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$id": "pbxapi.subscribepresenceresult.schema.json",
  "description": "Message sent back to confirm start of the monitoring",
  "type": "object",
  "required": [
    "api",
    "mt"
  ],
  "properties": {
    "api": {
      "type": "string",
      "enum": [
        "PbxApi"
      ],
      "description": "To address this interface the value 'PbxApi' has to be used for the api property"
    },
    "mt": {
      "type": "string",
      "enum": [
        "SubscribePresenceResult"
      ],
      "description": "The message type identifies the requested operation"
    }
  },
  "additionalProperties": false
}